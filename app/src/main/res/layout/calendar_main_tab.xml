<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:animateLayoutChanges="true"
    android:id="@+id/main_content"
    >

    <com.example.jonathan.iotmedicineapp.Calendar.CompactCalendarView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/compactcalendar_view"
        android:layout_width="fill_parent"
        android:layout_height="250dp"
        app:compactCalendarTargetHeight="250dp"
        app:compactCalendarTextSize="12sp"
        app:compactCalendarBackgroundColor="#93DBCF"
        app:compactCalendarTextColor="#686868"
        app:compactCalendarCurrentSelectedDayBackgroundColor="#37bcac"
        app:compactCalendarCurrentDayBackgroundColor="#E0E0E0"
        app:compactCalendarCurrentDayIndicatorStyle="fill_large_indicator"
        app:compactCalendarEventIndicatorStyle="small_indicator"
        app:compactCalendarOtherMonthDaysTextColor="#534c4c"
        />


    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="fill_parent"
        android:id="@+id/calendar_control_buttons_2"
        android:layout_below="@+id/calendar_control_buttons"
        android:layout_height="wrap_content">

    </LinearLayout>

    <ListView
        android:id="@+id/bookings_listview"
        android:layout_width="wrap_content"
        android:layout_height="250sp"
        android:layout_below="@+id/compactcalendar_view"
        android:layout_alignParentStart="true">
    </ListView>

</RelativeLayout>
